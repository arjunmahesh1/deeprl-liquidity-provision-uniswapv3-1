# Generated data and outputs. Every pattern here is ANCHORED to the repo root with a
# leading slash. A bare `data/` matches a directory named `data` at ANY depth, which
# silently excluded the source package's own data module
# (src/deeprl_liquidity_provision_uniswapv3/data/: the pool registry, the hourly
# aggregation, the swap-level fee model) from the repository. It was never committed
# and `git status` stayed clean while the code that builds the panel was missing.
/data/**
/outputs/
/old/
/results/
/figs/
/paper/
/RLFin_competition/

# ...but the panel's documentation belongs in the repo, not just on the machine that
# built it. Re-admitting files under an excluded directory needs the directories
# themselves re-admitted first, or git never descends far enough to see them.
!/data/
!/data/**/
!/data/**/*.md
!/data/**/CHECKSUMS.txt


# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# probe outputs and scratch
.DS_Store
__pycache__/
*.pyc

# --- internal working documents: never in the repo ---
# The repo lives on GitHub and was public until 2026-07-17. Anything committed here
# is reachable forever from every fork in the network, and privatising the parent does
# NOT retract it from a public fork. These stay on disk and are shared out of band.
#
# reports/          correspondence and revision memos. Not ours to publish.
# DIVERGENCES.md    internal audit notes.
# SPEC.md           pre-registration and running results.
# CLAUDE.md         local assistant/project instructions, not part of the project.
reports/
DIVERGENCES.md
SPEC.md
CLAUDE.md
